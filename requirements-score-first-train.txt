# Install torch separately from the CUDA-specific PyTorch index in the GPU
# bootstrap script. Unbounded torch installs can select a CUDA runtime newer
# than the NVIDIA driver exposed by a rented host.
transformers>=4.46,<5
peft>=0.13,<1
bitsandbytes>=0.43,<1
accelerate>=1.0,<2
